import './app.css';

function App(): JSX.Element {
  return (
    <main>
      <h1>AMA Archive Frontend</h1>
      <p>Configure environment variables in <code>.env</code> to customize API access.</p>
    </main>
  );
}

export default App;
